export type Participant = {
  id: number,
  firstName: string,
  lastName: string,
  joinDate: string,
}

export type ParticipantBase = {
  firstName: string,
  lastName: string,
  joinDate: string,
}